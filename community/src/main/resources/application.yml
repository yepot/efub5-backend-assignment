spring:
  application:
    name: community

  datasource:
    url: jdbc:mysql://localhost:3306/community?useSSL=false&useUnicode=true
    username: root
    password: mdmstj2395!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8000